# Churro Force

## What this is

TBD — fill in once the project's purpose is settled.

## Environment

- Windows 11, PowerShell 5.1 is the primary shell.
  - No `&&` / `||` chaining. Use `;` or `if ($?) { ... }`.
  - No ternary, `??`, or `?.` operators.
- The project path contains a space (`C:\Users\LENOVO\Churro Force`) — always quote paths in shell commands.

## Stack

Not chosen yet. When one is picked, record here:

- Language / runtime:
- Package manager:
- Test runner:
- Lint / format:

## Commands

| Task  | Command |
| ----- | ------- |
| Build | TBD     |
| Test  | TBD     |
| Lint  | TBD     |
| Run   | TBD     |

## Conventions

- Keep `README.md` accurate as the stack lands.
- Secrets go in `.env` (gitignored); commit an `.env.example` with keys but no values.
